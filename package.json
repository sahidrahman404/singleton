{
  "type": "module",
  "name": "singleton",
  "scripts": {
    "build": "esbuild src/*.ts --bundle=false --platform=node --outdir=dist --format=esm",
    "build-browser": "esbuild src/*.ts --bundle=false --platform=browser --outdir=browser --format=esm",
    "start": "node dist/index.js"
  },
  "packageManager": "yarn@4.2.2",
  "devDependencies": {
    "@types/node": "^22.4.0",
    "esbuild": "^0.23.1",
    "prettier": "^3.3.3",
    "tsc": "^2.0.4",
    "typescript": "^5.5.4"
  }
}
